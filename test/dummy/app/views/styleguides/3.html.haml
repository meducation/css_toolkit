= kss_block '3.1.1' do |modifier_class|
    -if modifier_class
        -modifier_class = modifier_class[1..-1]
        %div{class: modifier_class} #{modifier_class.capitalize!.gsub('-', ' ').gsub('bg', 'Background')}
    -else
        %div No Color

= kss_block '3.1.2' do |modifier_class|
    -if modifier_class
        -modifier_class = modifier_class[1..-1]
        %div{class: modifier_class} #{modifier_class.capitalize!.gsub('-', ' ').gsub('bg', 'Background')}
    -else
        %div No Color

= kss_block '3.1.3' do |modifier_class|
    -if modifier_class
        -modifier_class = modifier_class[1..-1]
        %div{class: modifier_class} #{modifier_class.capitalize!.gsub('-', ' ').gsub('bg', 'Background')}
    -else
        %div No Color

= kss_block '3.1.4' do |modifier_class|
    -if modifier_class
        -modifier_class = modifier_class[1..-1]
        %div{class: modifier_class} #{modifier_class.capitalize!.gsub('-', ' ').gsub('bg', 'Background')}
    -else
        %div No Color

= kss_block '3.2.1' do |modifier_class|
    -if modifier_class
        -modifier_class = modifier_class[1..-1]
        %div{class: modifier_class} #{modifier_class.capitalize!.gsub('-', ' ').gsub('bg', 'Background')}
    -else
        %div No Color

= kss_block '3.2.2' do |modifier_class|
    -if modifier_class
        -modifier_class = modifier_class[1..-1]
        %div{class: modifier_class} #{modifier_class.capitalize!.gsub('-', ' ').gsub('bg', 'Background')}
    -else
        %div No Color
